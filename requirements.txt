# ============================================================
# Flash Sale System - Python Dependencies
# ============================================================

# Web Framework
fastapi==0.115.6              # Modern web framework with auto API docs
uvicorn[standard]==0.34.0     # ASGI server to run FastAPI

# Database - PostgreSQL
sqlalchemy==2.0.36            # ORM (Object-Relational Mapping)
psycopg2-binary==2.9.10       # PostgreSQL driver
alembic==1.14.0               # Database migration tool (optional, for schema changes)

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT token generation and validation
passlib[bcrypt]==1.7.4            # Password hashing with bcrypt
bcrypt==4.0.1                     # 釘住版本，4.1+ 與 passlib 1.7.4 不相容
python-multipart==0.0.18          # Support for form data (login forms)

# Redis - Caching & Atomic Operations
redis==5.2.1                  # Redis client for Python

# RabbitMQ - Message Queue
pika==1.3.2                   # RabbitMQ client (AMQP)

# Configuration & Environment
python-dotenv==1.0.1          # Load .env file
pydantic==2.10.5              # Data validation (included with FastAPI)
pydantic-settings==2.7.1      # Settings management from .env

# Development & Testing (Optional)
pytest==8.3.4                 # Testing framework
pytest-asyncio==0.24.0        # Async support for pytest
httpx==0.28.1                 # HTTP client for testing FastAPI
locust==2.32.5                # Load testing tool (for Day 6)

# Utilities
email-validator==2.2.0        # Email validation for user registration
